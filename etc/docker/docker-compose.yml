version: '3.7'

services:

  # Container just for unit tests of our core code at src folder
  laravel-ddd-example.app:
    build:
      context: ./../../
      dockerfile: ./etc/docker/php.Dockerfile
    container_name: laravel-ddd-example.app
    env_file:
      - ./../../.env
    environment:
      SERVICE_NAME: app
      SERVICE_TAGS: dev
    networks:
      - laravel-ddd-example.network
    restart: unless-stopped
    tty: true
    volumes:
      - ./../../:/var/www
      - ./../../etc/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    working_dir: /var/www

  laravel-ddd-example.db:
    container_name: laravel-ddd-example.db
    env_file:
      - ./../../.env
    environment:
      MYSQL_DATABASE: ${DB_DATABASE-example}
      MYSQL_USER: ${DB_USERNAME-example}
      MYSQL_PASSWORD: ${DB_PASSWORD-secret}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD-secret}
      SERVICE_NAME: mysql
      SERVICE_TAGS: dev
    image: mysql:latest
    networks:
      - laravel-ddd-example.network
    restart: unless-stopped
    tty: true
    volumes:
      - laravel-ddd-example.dbdata:/var/lib/mysql/
      - ./../../etc/mysql/my.cnf:/etc/mysql/my.cnf

  laravel-ddd-example.prometheus:
    container_name: laravel-ddd-example.prometheus
    image: prom/prometheus:v2.36.1
    volumes:
      - ./../../etc/prometheus/:/etc/prometheus/
      - laravel-ddd-example.prometeus:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    networks:
      - laravel-ddd-example.network
    extra_hosts:
      - "host.docker.internal: host-gateway"
    ports:
      - 9999:9090

  laravel-ddd-example.grafana:
    container_name: laravel-ddd-example.grafana
    image: grafana/grafana:7.5.7
    networks:
      - laravel-ddd-example.network
    ports:
      - 3001:3000
    restart: unless-stopped
    volumes:
      - ./../../etc/grafana/provisioning/datasources:/etc/grafana/provisioning/datasources
      - laravel-ddd-example.grafana:/var/lib/grafana

  laravel-ddd-example.kanban-api:
    build:
      context: ./../../
      dockerfile: ./etc/docker/php.Dockerfile
    command: php -S laravel-ddd-example.kanban-api:8080 -t ./apps/kanban-api/public
    container_name: laravel-ddd-example.kanban-api
    depends_on:
      - laravel-ddd-example.db
      - laravel-ddd-example.prometheus
    env_file:
      - ./../../.env
    environment:
      SERVICE_NAME: app
      SERVICE_TAGS: dev
    networks:
      - laravel-ddd-example.network
    ports:
      - 8080:8080
    restart: unless-stopped
    tty: true
    volumes:
      - ./../../:/var/www
      - ./../../etc/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    working_dir: /var/www

networks:
  laravel-ddd-example.network:
    name: laravel-ddd-example.network
    driver: bridge

volumes:
  laravel-ddd-example.dbdata:
    name: laravel-ddd-example.dbdata
    driver: local
  laravel-ddd-example.prometeus:
    name: laravel-ddd-example.prometeus
    driver: local
  laravel-ddd-example.grafana:
    name: laravel-ddd-example.grafana
    driver: local
